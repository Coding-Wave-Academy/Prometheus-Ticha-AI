export default function GettingStartedLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div>
      {/* TODO: Add getting started specific navigation/sidebar */}
      {children}
    </div>
  );
}
