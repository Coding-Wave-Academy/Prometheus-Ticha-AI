export default function CourseLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div>
      {/* TODO: Course-specific layout with sidebar module navigation */}
      {children}
    </div>
  );
}
